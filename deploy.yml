name: Shuttle Deploy

on:
  workflow-dispatch:

  jobs:
    publish:
      runs-on: ubuntu-latest
      timeout-minutes: 30
      steps:
        - uses: shuttle-hq/deploy-action@main
          with:
          deploy-key: ${{ secrets.SHUTTLE_DEPLOY_KEY }}
